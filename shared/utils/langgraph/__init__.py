"""LangGraph agent runtime (AGENT_FRAMEWORK=langgraph).

Serves the same HTTP/SSE wire contract as the ADK server (adk_main.py) so the
auth server, dashboard, widget and OpenAI-compat layers work unchanged.
"""

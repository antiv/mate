"""
Server package for MATE (Multi-Agent Tree Engine).

Splits the auth server into modular components:
- auth: Authentication dependencies and helpers
- auth_routes: Token generation, login/logout endpoints
- proxy_routes: ADK proxy, WebSocket, and documentation proxy endpoints
"""
